apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: nginx
  namespace: nginx
spec:
  chart:
    spec:
      chart: nginx-ingress
      version: 2.3.x
      sourceRef:
        kind: HelmRepository
        name: nginx
      interval: 5h
  interval: 5h
  releaseName: nginx